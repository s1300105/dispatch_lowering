# OpenManus: the LLM's tool-call message is attacker-influenced (prompt injection); its
# content selects and parameterises the tool executed by ToolCollection.execute.
def app.llm.LLM.ask_tool(self, messages, system_msgs, timeout, tools, tool_choice, temperature, **kwargs) -> TaintSource[LLMControlled]: ...
def app.llm.LLM.ask(self, messages, system_msgs, stream, temperature) -> TaintSource[LLMControlled]: ...
